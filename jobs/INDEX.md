# Job Queue

Tracks the breakdown of [`PLAN.md`](../PLAN.md) into worker-chat-sized jobs. One job runs at a time — a job's status must reach **Done** (or be explicitly abandoned) before the next one starts. Update the **Status** column as jobs progress; each job also carries its own status line in its file.

Every job file is self-contained: it names its PLAN.md section(s), its dependencies, its deliverables, and its acceptance criteria, so a fresh worker chat with no prior context can pick it up.

| # | Job | Phase | Depends on | Status |
|---|---|---|---|---|
| 001 | [Monorepo scaffold](001-monorepo-scaffold.md) | 0 · Foundations | — | Done |
| 002 | [`packages/rational`](002-rational-package.md) | 0 · Foundations | 001 | Done |
| 003 | [`packages/gamedata`](003-gamedata-package.md) | 0 · Foundations | 001 | Done |
| 004 | [Core DB migrations](004-db-migrations-core.md) | 1 · Auth & projects | 001 | Done |
| 005 | [Discord OAuth2](005-discord-oauth.md) | 1 · Auth & projects | 004 | Done |
| 006 | [Project list UI](006-project-list-ui.md) | 1 · Auth & projects | 005 | Done |
| 007 | [`packages/ydoc` schema](007-ydoc-schema.md) | 2 · Solo canvas editor | 001 | Done |
| 008 | [Canvas skeleton](008-canvas-skeleton.md) | 2 · Solo canvas editor | 007 | Done |
| 009 | [Recipe Chooser](009-recipe-chooser.md) | 2 · Solo canvas editor | 008, 003 | Done |
| 010 | [Recipe node UI](010-recipe-node-ui.md) | 2 · Solo canvas editor | 009 | Done |
| 011 | [Connections & waypoints](011-connections-waypoints.md) | 2 · Solo canvas editor | 010 | Done |
| 012 | [Selection & editing](012-selection-editing.md) | 2 · Solo canvas editor | 011 | Done |
| 013 | [Outposts](013-outposts.md) | 2 · Solo canvas editor | 012 | Done |
| 014 | [Visual pass & theming](014-visual-pass.md) | 2 · Solo canvas editor | 013 | Done |
| 015 | [Doc persistence](015-doc-persistence.md) | 3 · Persistence | 014, 006 | Done |
| 016 | [IndexedDB cache & versions](016-indexeddb-versions.md) | 3 · Persistence | 015 | Done |
| 017 | [Solver core](017-solver-core.md) | 4 · Calculators | 002 | Done |
| 018 | [Solver worker host](018-solver-worker.md) | 4 · Calculators | 017, 016 | Done |
| 019 | [Summary panel & formats](019-summary-panel.md) | 4 · Calculators | 018 | Done |
| 020 | [Hocuspocus server](020-hocuspocus-server.md) | 5 · Multiplayer | 019 | Done |
| 021 | [Presence](021-presence.md) | 5 · Multiplayer | 020 | Done |
| 022 | [Integrity reducer & sharing](022-integrity-reducer.md) | 5 · Multiplayer | 021 | Done |
| 023 | [Full calculator](023-full-calculator.md) | 6 · Full calculator | 022 | Done |
| 024 | [Priority node types](024-priority-nodes.md) | 6 · Full calculator | 023 | Done |
| 025 | [Relational projection](025-relational-projection.md) | 6 · Full calculator | 024 | Not started |
| 026 | [Blueprints](026-blueprints.md) | 7 · Polish & deploy | 025 | Not started |
| 027 | [Auto-round, styles, minimap](027-polish-misc.md) | 7 · Polish & deploy | 026 | Not started |
| 028 | [i18n wiring](028-i18n.md) | 7 · Polish & deploy | 027 | Not started |
| 029 | [Accessibility & deploy](029-a11y-deploy.md) | 7 · Polish & deploy | 028 | Not started |

**Phase 1 (Auth & projects) is now fully complete** as of Job 006 — Discord OAuth2 login, sessions, and project CRUD (create/rename/duplicate/soft-delete) with role enforcement are all in place. Phase 2 (Solo canvas editor) starts next at Job 007, which builds `packages/ydoc`'s CRDT schema independently of any of Job 006's routes, but depends on `projects` existing (Job 004) to eventually attach documents to.

**Phase 2 (Solo canvas editor) is now fully complete** as of Job 014 — a local-Yjs-doc React Flow canvas with the Recipe Chooser, real recipe node UI, drag-to-connect with waypoints, marquee select/cut/copy/paste/delete/undo/redo, outposts with drill-in and breadcrumbs, a Ferrumium-inspired visual pass with dark/light theming, and snap-to-grid for both machines and waypoints are all in place. Phase 3 (Persistence) starts next at Job 015, which needs `Settings` (and the rest of the document) to actually survive a reload.

**Phase 3 (Persistence) is now fully complete** as of Job 016 — Yjs↔Postgres snapshot+log persistence with compaction (Job 015), plus `y-indexeddb` local caching (instant render from cache on reload, reconciled against the server), a live autosave-status indicator with auto-retry, and `project_versions` auto/manual snapshots with a working restore flow (Job 016) are all in place. Phase 4 (Calculators) starts next at Job 017, which is pure solver logic with no dependency on the canvas/persistence track beyond `packages/rational`.

**Job 017 (`packages/solver`) is done** — None/Manual/Basic calculators over exact `Rational` arithmetic, with a documented deterministic fixed-point propagation algorithm for Basic mode (PLAN.md §5 point 4's requirement), all four PLAN.md §9 golden-value tests passing exactly, and zero Yjs/React/DOM dependency. Phase 4 continues at Job 018, which wraps `solve()` in a cancellable/debounced Web Worker host.

**Job 018 (solver worker host) is done** — a Web Worker (`apps/web/src/workers/solverWorker.ts`) hosting `@scm/solver`'s `solve()`, plus a main-thread `useSolver(sfmDoc)` hook/scheduler that debounces edits (~150ms), genuinely cancels a superseded in-flight solve via `Worker.terminate()` (with a pre-warmed spare worker so cancellation never re-pays `@scm/gamedata`'s module-init cost), and partitions the real underlying node/edge graph (never a container-scoped slice) into connected components so an edit only re-solves the component(s) it actually touched. Verified live in a browser: dirty-subgraph invalidation is byte-precise, rapid edits collapse to one final solve, and a synthetic 500-node/808-edge Basic solve completes in ~14-73ms end to end through the real worker — well under PLAN.md §9's 200ms budget. Phase 4 continues at Job 019, which builds the real summary panel/highlighting UI on top of `useSolver`'s `{result, staleness, nodeResultById, edgeResultById}`.

**Job 019 (summary panel, validity highlighting & number formats) is done** — real solver output replaces Job 010's stopgap rate display on every node card; red ("invalid," e.g. an unparseable limit) vs orange ("mismatched," e.g. a Basic-mode split/merge rate disagreement) highlighting on the affected fields/ports, verified live to coexist correctly on one node without conflating; a real summary panel (made/used/unmade/unused, power made/used/net, sink points, cost-to-build) scoped to Everything/Current Outpost/Selected, each scope re-deriving its aggregate from the raw per-node solver output rather than slicing an already-aggregated summary; a number-format settings UI wired straight through `@scm/rational`'s `formatRational`, confirmed to re-render every displayed value live with no reload; and Job 018's staleness state visibly greying the UI during recompute, precisely timed via a live poll (grey from ~58ms to ~178ms around a real edit). A genuine duplicate-worker-scheduler bug (`DevNodeTools.tsx`'s pre-existing diagnostic panel calling `useSolver` a second time, independently of this job's own single call site) was found and fixed live. **Phase 4 (Calculators) is now fully complete.** Phase 5 (Multiplayer) starts next at Job 020, which builds the Hocuspocus server largely independently of this job's UI work — no solver output ever touches the CRDT, so nothing here changes what Job 020 needs to sync.

**Job 020 (Hocuspocus server, ticket auth & roles) is done** — `GET /api/realtime/ticket` mints a 60-second signed connection ticket; a real `@hocuspocus/server` in `apps/realtime` verifies it, re-checks the caller's role against Postgres at connect time, and sets genuine server-side `readOnly` for viewers, verified with an explicit test required by PLAN.md §9: a raw, client-UI-bypassing `@hocuspocus/provider` connection with a real viewer ticket attempts a write, and the persisted document — read back independently — is unchanged. `apps/web`'s live-sync transport now runs over a real WebSocket via `@hocuspocus/provider` (fetching a fresh ticket immediately before every connect/reconnect) instead of Job 015/016's debounced-REST push loop, with the `y-indexeddb` local-cache layer and its instant-render-then-reconcile behavior left intact. Job 015's persistence module (load/append/compact/versions) was promoted, not duplicated, into a new shared package (`packages/doc-storage`) so `apps/realtime` reuses the exact same code `apps/api`'s REST routes do; that same move also caught and fixed a real, previously-only-theoretical production-build gap Job 001 had flagged — a plain `node dist/*.js` can't execute a JIT `packages/*`-style dependency once it has internal relative imports, fixed by having `packages/doc-storage` ship its built `dist/` instead, with `turbo.json` updated so building it first is automatic, never a manual step. Revocation (a role change or removal force-disconnecting the affected live session, verified end to end down to ~10ms) works via a small internal-only webhook `apps/api` calls on `apps/realtime` plus an hourly re-verification sweep as the backstop, exercised through the real REST member-management routes this job also had to add (a deliberately minimal owner-only role/removal API — Job 022's "sharing" scope should extend it, not replace it). **Phase 5 (Multiplayer) has begun.** Phase 5 continues at Job 021 (presence), which layers Yjs Awareness (cursors/avatars/selection) on top of this now-real transport — see Job 020's own Handoff notes for exactly what's already in place (the live `HocuspocusProvider` instance inside `useProjectDocument.ts`, the per-connection `{userId, role}` context on the server) versus what Job 021 still needs to wire up itself.

**Job 021 (Presence) is done** — `apps/web/src/collab/` (a placeholder since Job 001) now carries the real Yjs Awareness wire-up: `provider.awareness` threaded through `CanvasDocContext` alongside `sfmDoc`/`undoManager`; a deterministic per-`userId` color hash; container-scoped live cursors (a peer's cursor only renders when the local viewer is looking at the same outpost, both directions verified); colored selection halos on `RecipeNode`/`OutpostNode` cards; a top-bar avatar list (every connected role, including viewers); and soft (never-blocking) ring+avatar indicators on the limit/clock/shards fields, wired to focus/blur. Verified with 35 new pure-logic unit tests (`apps/web`, 217 → 250) plus two real, independent `@hocuspocus/provider` clients converging over the real server in a new `apps/realtime/src/presence.test.ts` (11 → 13), including an automated disconnect-cleanup proof that a peer's full presence footprint clears with zero stranded state — and then the same disconnect-cleanup check again, manually, through a real browser tab plus a real second Node-side provider client (this sandbox's single-origin limit blocks two cookie-authenticated browser tabs, same workaround Job 020 used), alongside live checks of the avatar list, cursor, selection halo, field indicator, and concurrent (non-blocked) editing of the same field. Repo-wide: 525 tests passing. **Phase 5 (Multiplayer) continues** at Job 022 (integrity reducer & sharing), which needs to know presence never touches Postgres/the CRDT at all (confirmed, no overlap with the reducer pass) and that `memberRoutes.ts`'s existing force-disconnect-on-revocation already clears a removed member's presence for free, with no extra work — see Job 021's own Handoff notes for the full detail.

**Job 022 (Integrity reducer & sharing) is done — Phase 5 (Multiplayer) is now fully complete, completing the MVP** (PLAN.md §8: "Phases 0-5 are the MVP"). `packages/ydoc/src/integrity.ts` implements all four PLAN.md §5 repair rules (delete dangling edges; reparent — never delete — orphaned nodes/containers/edge-containers to root, generalizing Job 013's `deleteOutpost` reparent pattern to fire from *any* cause, not just a user's own explicit delete; clamp shards to `MaxProductionShards`/drop invalid ports via real `@scm/gamedata` lookups; dedupe edges as a backstop), run inside the `origin: 'integrity'` transaction Job 007 reserved, wired into both `apps/web`'s local transaction pipeline (`afterTransaction` listener + repair-on-load) and `apps/realtime`'s document lifecycle (`afterLoadDocument` + `onStoreDocument`, repairing before every persist — verified with a real test that a raw client's dangling-edge write never lands in `project_doc_state`/`project_doc_updates`). A 300-sequence CRDT convergence fuzz test (`packages/ydoc/src/fuzz.test.ts`) plus a real two-client delete-vs-connect race test (`apps/realtime/src/integrity.test.ts`) both pass, asserting byte-identical `Y.encodeStateAsUpdate` convergence and zero invariant violations. Real sharing UI now exists end to end: `project_invites` (unused since Job 004) now backs invite creation/listing/revocation (`apps/api/src/projects/inviteRoutes.ts`, owner-only) and public preview/redemption (`GET/POST /api/invites/:token[/redeem]`, with an atomic `uses`-increment verified safe under real concurrent redemption), a `SharingPanel` in `apps/web` extending Job 020's minimal `memberRoutes.ts` with a full member-management + invite-link UI, and an `InviteRedeemPage` reached via `/i/:token` share links (client-side-only routing, since this app has no router — the token survives the Discord-login redirect via `sessionStorage`). A distinct `ConnectionStatusIndicator` (`connected`/`reconnecting`/`offline`) was added alongside the existing `SaveStatusIndicator`, deliberately kept as two separate signals (transport state vs. save-durability state) — both verified live in a real browser against a real killed-and-restarted `apps/realtime` process. Repo-wide: **559 tests passing** (`rational` 67, `gamedata` 40, `doc-storage` 24, `solver` 17, `ydoc` 43, `api` 100, `realtime` 15, `web` 253); `pnpm -r build`/`typecheck`/`test`, `pnpm lint` all clean. One notable side effect: `apps/realtime` importing `@scm/ydoc` for the first time (which transitively needs `@scm/gamedata`/`@scm/rational`) surfaced the same `NodeNext`-vs-`Bundler` extension-completeness gap Job 020 first hit with `@scm/doc-storage` — fixed at the narrowest scope (added `.js` extensions to those three packages' internal imports; did NOT convert them to ship a `dist/` the way `doc-storage` did, since all three still have `apps/web` as a real JIT/Bundler consumer) — see Job 022's own Handoff notes for the one still-open, not-yet-reachable gap this leaves for Phase 7's eventual deploy/production-packaging work. **Phase 6 (Full calculator) starts next at Job 023**, described as largely independent of this job — see Job 022's Handoff notes for the (minimal) points of overlap worth knowing about.

**Job 023 (Full calculator) is done.** `packages/solver` gained a `"full"` `SolverMode`: even-split preference and two-tier priority routing ("top drains first, bottom takes overflow") at any splitter/merger, formulated as an exact-rational LP — solved per sibling group via max-min-fair water-filling (`waterFill.ts`), a closed-form/greedy algorithm for that specific LP rather than a general-purpose simplex tableau, applied inside the same Jacobi-style pass-propagation architecture Job 017 built for Basic mode. Priority metadata is represented as a new optional `SolverEdge.priorityTier?: "top" | "bottom"` field (additive to Job 017's "Done" `snapshot.ts`, `undefined` behaving exactly like `"top"` so every existing snapshot is unaffected) — flagged as this job's one genuinely uncertain design call, since it's a single direction-agnostic bit rather than separate split-side/merge-side metadata; see Job 023's own Handoff notes before Job 024 locks in its priority-node UI against it. Cooperative mid-solve cancellation and progress reporting (`FullSolveOptions { signal, onProgress }`, a `CancellationSignal` structurally compatible with a real `AbortSignal`) check in at every propagation pass, every splitter/merger group, and every water-fill round — verified with a hand-derived 3-round staircase cancelled mid-loop, plus a 500+-group synthetic graph cancelled after only a handful of progress callbacks, returning a defined `{ cancelled: true }` result rather than throwing or running to completion. No real desktop-tool reference data was available in this sandbox (no network access); every benchmark in `full.golden.test.ts` is hand-derived from PLAN.md's own stated rules on small, hand-checkable graphs instead, the same approach Job 017's golden tests used. `pnpm --filter @scm/solver test` — 10 files, 39 tests, all passing (stable across repeated runs); zero `number` leakage into the constraint/objective/solution path outside the pre-existing power boundary, confirmed by direct grep+inspection. `pnpm -r build`/`typecheck`, `pnpm lint` all clean; `apps/realtime`/`apps/api`'s pre-existing Postgres-credential environment issue (unrelated — neither depends on `@scm/solver`) is the only repo-wide test failure, matching the same class of environment gap Jobs 017/018/022 already flagged. **Phase 6 continues at Job 024** (priority node types & STOP UI), which builds the dedicated Priority Splurger node type and progress/cancel UI on top of this job's `priorityTier`/`FullSolveOptions` — see Job 023's Handoff notes for the full API surface and its documented limitations.

**Job 024 (Priority Splurger node type & STOP/progress UI) is done.** A real, persisted `kind: "splurger"` node (`apps/web/src/canvas/nodes/SplurgerNode.tsx`) is creatable via a new "+ New Splurger" Recipe Chooser entry point, with two generic wildcard-part ports (`WILDCARD_PART`/`reconcilePart`, a small additive extension to Job 011's "Done" `connectionLogic.ts`), a live connection list with top/bottom tier toggles and within-tier reordering, and a visible warning for the one wiring shape it deliberately doesn't model (multiple inputs AND multiple outputs at once — which has no correct rewrite and also has no real Satisfactory hardware equivalent). The genuinely uncertain design call this job had to resolve: since `@scm/solver`'s snapshot has no node type for a pure routing node, `apps/web/src/workers/splurgerPassthrough.ts` erases a Splurger before it ever reaches the solver, rewriting its incident edges into direct recipe-to-recipe `SolverEdge`s carrying its tier assignment (reusing Job 007's already-defined, previously-unused `NodeRecord.priorityOrder` — **zero `@scm/ydoc` schema or mutation changes needed**) — exact, not approximate, for the two shapes that matter (1-input/N-output and N-input/1-output), with **zero changes to `packages/solver`**, verified via `git diff --stat`. Job 018/023's worker host (`solveScheduler.ts`/`useSolver.ts`/`solverWorker.ts`/`protocol.ts`) now genuinely dispatches `"full"` mode (previously silently downgraded to `"none"`), relays live progress (`FullProgressInfo`, real-time even mid-solve — worker→main `postMessage` is never blocked by the worker's own busy thread) into a new "Solving… [STOP]" `SolveStatusIndicator`, and a new `stop()` — flagged prominently in this job's own Handoff notes as a genuinely uncertain call worth Job 025+ knowing about: the cooperative `cancel` message Job 023 suggested is wired end-to-end but, in this specific synchronous single-threaded worker, **cannot actually interrupt an in-flight `solve()` call** (a message arriving mid-computation just queues until the call already returns) — `Worker.terminate()` (Job 018's real, engine-level preemption) is what genuinely halts computation today, confirmed live against a real `Worker` and a real 6000-node synthetic graph (uncancelled: still running after 10s; cancelled ~16ms in: no result ever landed in a 3s follow-up wait). Priority routing correctness was confirmed live end-to-end (real UI → real worker → real `solveFull`) via `window.__solveDebug`: an uncapped top-tier consumer drained a producer's full 60/min supply while the bottom-tier sibling received exactly 0, and — with top's own demand reduced to exactly what it needed — top got its complete demand while bottom correctly received the remaining genuine overflow, both edges valid with zero mismatch. One real, live-discovered gap flagged for a later job: a Splurger-mediated connection's rate-mismatch/overflow is computed correctly but doesn't visually highlight on the real recipe nodes' ports, because the solver-level synthetic edge id never matches the real `EdgeRecord.id` Job 019's `computeValidity.ts` looks up by — the numbers are right, only the red/orange decoration doesn't reach through a Splurger yet. `apps/web` test count 253 → 284 (31 new, `splurgerPassthrough.test.ts` plus extensions to `connectionLogic`/`buildSnapshot`/`solveScheduler` test suites); `pnpm -r build`/`typecheck` clean across all 10 workspaces; `pnpm lint` clean; `apps/realtime`'s own suite (untouched by this job) showed one flaky WebSocket-timing test late in the session, plausibly from this job's own heavy browser-based load-testing, consistent with the class of pre-existing environment flakiness prior jobs have already documented. **Phase 6 continues at Job 025** (relational projection), which per PLAN.md §4 needs `proj_nodes.kind` to include `'splurger'` (already true — Job 007's `KNOWN_NODE_KINDS` has carried it since before this job existed) and should be aware that a Splurger's own `priorityOrder` field now has real, meaningful content (the tier-assignment encoding this job defined) rather than being permanently empty.

## Conventions for every job file

- **Depends on** means those jobs' deliverables must exist and pass their acceptance criteria first.
- **Out of scope** lines exist to stop a worker from scope-creeping into the *next* job — that's a feature, not a gap.
- Workers should update their own job file's **Status** line (`Not started` → `In progress` → `Done`) and this table when they finish, and leave a short **Handoff notes** section if anything relevant to later jobs was discovered mid-work.
- Jobs 020+ assume 015/016 (persistence) already landed, since multiplayer without persistence is pointless to build against.
