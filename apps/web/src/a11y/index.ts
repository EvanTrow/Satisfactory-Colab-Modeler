// Job 029: barrel for the accessibility-pass utilities — mirrors every
// other feature directory's own `index.ts` convention (`theme/`, `i18n/`,
// `collab/`, etc.).
export { getFocusableElements, useFocusTrap, type UseFocusTrapOptions } from "./useFocusTrap";
