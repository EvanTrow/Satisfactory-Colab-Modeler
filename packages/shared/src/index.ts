// Placeholder workspace member.
// TODO: zod schemas + types shared by apps/web and apps/api (auth payloads,
// project/member DTOs, ticket claims, etc). Filled in incrementally by the
// jobs that need it (auth, persistence, ...). See PLAN.md §7.

export {};
