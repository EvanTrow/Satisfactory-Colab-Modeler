// Placeholder workspace member.
// TODO(Job 007): the Y.Doc CRDT schema (meta/settings/containers/nodes/
// edges), mutation helpers, the integrity reducer, and schema migrations.
// Imported by both apps/web and apps/realtime so client and server never
// drift apart. See PLAN.md §4 "The CRDT document schema" and §7, and
// jobs/007-ydoc-schema.md.

export {};
