// Placeholder workspace member.
// TODO(Job 017): the calculators (Manual, Basic, None; Full in Job 023+) as
// pure functions over a plain snapshot -> plain results. No DOM, no Yjs, no
// network -- must run identically in a Web Worker, in Node tests, and on
// the server. See PLAN.md §7 and jobs/017-solver-core.md.

export {};
